name: Zip and Commit

on:
  push:
    branches:
      - main  # Trigger on push to the main branch

jobs:
  zip_and_commit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Clean up old ZIP files (if any)
        run: |
          # Remove any existing ZIP file to avoid conflicts or overwriting issues
          rm -f 3pad-playground.zip

      - name: Zip the contents of the 3pad-playground folder
        run: |
          # Create a zip of the contents of the 3pad-playground directory
          cd 3pad-playground
          zip -r ../3pad-playground.zip .
        shell: bash

      - name: Set up Git
        run: |
          git config --global user.name 'GitHub Actions'
          git config --global user.email 'actions@github.com'

      - name: Commit ZIP to repository
        run: |
          # Check for changes, only commit if there are changes to avoid unnecessary commits
          if [ -n "$(git status --porcelain)" ]; then
            git add 3pad-playground.zip
            git commit -m "Add ZIP of 3pad-playground folder to root"
            git push
          else
            echo "No changes to commit"
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
